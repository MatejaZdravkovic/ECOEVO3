# EcoEvoCRM GUI Dependencies
# Install with: pip install -r requirements_gui.txt

# PyQt5 for GUI framework
PyQt5>=5.15.0

# Matplotlib for plotting (if not already installed)
matplotlib>=3.3.0

# NumPy for numerical operations (if not already installed)
numpy>=1.19.0

# SciPy for scientific computing (if not already installed)
scipy>=1.5.0
